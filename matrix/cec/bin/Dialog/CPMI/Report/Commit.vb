Imports System.Net

Public Class Commit
    Public Shared Sub SquidsProject (ByRef project As FileSystem)
        Dim writeFiles = project
        Dim fileSystem = writeFiles
        project = fileSystem
    End Sub
End Class